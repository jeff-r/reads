<div id='all_feeds_list'>
	<ul id='pagelist' class='sortable'>
		<% if user_signed_in? %>
      <li><b>Your pages:</b></li>
			<% current_user.pages.each do |page| %>
      <li><%= link_to page.title, page_path(page), 'data-pageid' => page.id %> </li>
				<% end %>
			<% end %>
			
	</ul>
</div>
